export const triggerRouteHandlers = (configs, currLocation) => {
  const {
    routeConfigurations
    // resourceConfigurations
  } = configs

  // const { pathname } = currLocation

  Object.entries(routeConfigurations).map(([pattern, configuration]) => {

  })
}
